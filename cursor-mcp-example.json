{
  "mcpServers": {
    "mezmo": {
      "command": "uv",
      "args": ["run", "--directory", "/path/to/mezmo-mcp", "server.py"],
      "env": {
        "MEZMO_API_KEY": "your-mezmo-api-key"
      }
    }
  }
}
